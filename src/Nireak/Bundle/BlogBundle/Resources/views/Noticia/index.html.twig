<h1>Noticia list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Izenburua</th>
            <th>Gorputza</th>
            <th>Argitaratze_data</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('noticia_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.Izenburua }}</td>
            <td>{{ entity.Gorputza }}</td>
            <td>{{ entity.Argitaratzedata|date('Y-m-d H:i:s') }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('noticia_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('noticia_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('noticia_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
